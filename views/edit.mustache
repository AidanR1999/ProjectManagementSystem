{{>header}}
{{! <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#addMilestone">
  Add milestone
</button> }}

<div class="container">
  {{#milestones}}
  
        <li>
            <div class="container">
                <div class="row">
                    <div class="container well span6">
                        <div class="col-sm"></div>
                        <div class="row-fluid">
                            <div class="span1">
                                <i class="fas fa-bars"></i>
                            </div>
                             <div class="row">
                                <div class="col-sm">
                                  <label><b>Milestone name:</b></label>
                                  <p>{{name}}</p>
                                </div>
                              </div>
                            <div class="span2">
                                <form action="/project/edit/{{projectId}}/deleteMilestone/{{_id}}" method="post" >
                                    <input type="hidden" id="id" name="id" value="{{_id}}">
                                    <button type="submit" class="btn btn-danger">
                                        <span class="icon-trash"></span> Delete</a>
                                    </button>
                                </form>
                            </div>
                        </div>
                        <div class="col-sm"></div>
                    </div>
                </div>
            </div>
        </li>
       
  {{/milestones}}
</div>

<div class="container">
        <div class="row">
            {{#project}}
            <form action="/project/edit/{{_id}}" method="post">
              <div class="form-group">
                  <label>Milestone Name:</label>
                  <input type="text" class="form-control" placeholder="Milestone Name" id="name" name="name">
              </div>
              <button type="submit" class="btn btn-primary">Add Milestone</button>
          </form>
                {{! <div class="form-group">
                  <label>Project Name:</label>
                  <input type="text" class="form-control" placeholder="Project Name" id="name" name="title">
                </div>
                <div class="form-group">
                    <label>Module:</label>
                    <input type="text" class="form-control" placeholder="Module" id="module" name="module">
                </div>
                <div class="form-group">
                    <label>Due Date:</label>
                    <input type="text" class="form-control" placeholder="MM/DD/YYYY" id="due" name="dueDate">
                </div> }}
            {{/project}}
        </div>
    
</div>

{{! <div class="modal fade" id="addMilestone" tabindex="-1" role="dialog" aria-labelledby="addMilestoneLabel" aria-hidden="true">
  {{! <form action="/project/edit/" method="post">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="addMilestoneLabel">Add Milestone</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          
          <div class="form-group">
              <label>Milestone Name:</label>
              <input type="text" class="form-control" placeholder="Milestone Name" id="name" name="title">
          </div>
          
        
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
          <button type="submit" class="btn btn-primary">Add Milestone</button>
        </div>
        
      </div>
    </div>
  </form> 
</div> }} 

{{>footer}}